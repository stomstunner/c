#include <stdio.h>
int main()
{
    float m1 = 72; // maths marks
    float m2 = 89; // scirnce marks
    float m3 = 94; // hindi marks
    float m4 = 93; // english marks
    float m5 = 96; // social science marks
    float p = (m1 + m2 + m3 + m4 + m5) / 5;
    printf("Percentage:%f", p);

    return 0;
}