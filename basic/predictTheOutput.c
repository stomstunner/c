#include<stdio.h>
int main(){
    int p,q,r,s;
    printf("Enter values of p, q, r and s respectively ");
    scanf("%d%d%d%d",&p,&q,&r,&s);
    printf("p = %d q = %d r = %d s = %d",p,q,r,s);
    return 0;
}